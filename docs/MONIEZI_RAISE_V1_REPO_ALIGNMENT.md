# MONIEZI Raise V1 — Repo Alignment

This source package is aligned to the GitHub repository name `moniezi-raise-v1`.

The service-worker cache name, package metadata, PWA manifest ID, and workflow marker comments are aligned to the Raise V1 identity.
